# API Configuration
# Backend API base URL - the proxy will forward requests here
# For production, make sure to use HTTPS to avoid Mixed Content errors
# Default: https://cvm.groupngs.com/api/database-service (production)
#          http://localhost:8080/api/database-service (development)
# API_BASE_URL=https://cvm.groupngs.com/api/database-service

# If your backend uses a specific port for HTTPS (like 8443), include it:
# API_BASE_URL=https://cvm.groupngs.com:8443/api/database-service

# Vite API Base URL (for direct client calls, if needed)
# VITE_API_BASE_URL=/api/proxy
